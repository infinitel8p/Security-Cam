---
sidebar_position: 1
last_update:
  date: 2026-03-31
  author: Ludo
---

# Using the Dashboard

After completing the setup, open the dashboard in your browser:

```
http://<raspberry-pi-ip>:3000
```

If you're connected to the Pi's Access Point, you can use any of these:

| URL | Note |
| --- | --- |
| `http://dashboard.cam` | Custom hostname, redirected via captive portal (no port needed) |
| `http://192.168.4.1:3000` | Direct IP access |
| `http://dashboard.cam:3000` | Custom hostname with explicit port |

:::tip
`http://dashboard.cam` (without port) works because the captive portal redirects port 80 to 3000. This is the easiest to remember but may be slightly slower due to the redirect.
:::

:::note
The frontend auto-detects the backend and MediaMTX URLs from the hostname you're visiting. No `.env` configuration is needed on the Pi. Only set `PUBLIC_BACKEND_URL` and `PUBLIC_MEDIAMTX_URL` in `server/.env` if you're running the frontend dev server on a separate machine.
:::

The dashboard is organized into three pages.

## Dashboard (home page)

The main page shows everything at a glance:

- **Live Feed** - real-time camera view via WebRTC, with manual recording controls (start/stop)
- **Backend Status** - online/offline indicator for the Flask backend, polled every 10 seconds
- **Sensor Status** - current sensor type, state (Armed / Triggered / Suppressed / Disabled), and GPIO pin. See [sensor states](./sensors#sensor-states) for what each means
- **Connection Status** - how many tracked Bluetooth and WiFi devices are online
- **System Health** - CPU temperature, load, RAM usage, storage, uptime, and throttle status
- **Activity Timeline** - last 7 days of events: sensor triggers, recording events, stream disconnects. Rapid sensor pulses within 5 minutes are collapsed into a single entry with a count
- **Access Log** - last 72 hours of device arrivals and departures
- **Health Tracker** - 72-hour system health chart (48 time slots, colour-coded ok/warn/critical)
- **Event Heatmap** - ~6-month activity heatmap showing event intensity by day of week
- **Setup Checklist** - first-run onboarding guide (add devices, enable sensor). Dismissible once complete

## Archive

Browse and manage recorded videos:

- Videos are displayed in a grid, grouped by date, and can be filtered by time range or sorted
- Click a video to stream it directly in the browser
- Download or delete individual recordings

## Settings

Configure the system without touching config files. The settings page has a sidebar with five sections:

- **Appearance** - switch between dark, light, and system theme. Choose the dashboard language (English, German, French, Spanish, Italian).
- **Camera** - adjust stream resolution, framerate, and rotation (display-only or hardware via MediaMTX). Toggle scan line overlay on the live feed.
- **Storage** - choose where recorded videos are stored on the Pi with a directory browser. Enable auto-delete to automatically remove the oldest recordings when disk usage exceeds a threshold.
- **Devices** - add or remove tracked Bluetooth and WiFi devices. Scan for nearby devices or enter MAC addresses manually. Online/offline status is shown per device. Tracked devices are used for presence detection - if any are nearby, sensor triggers are ignored.
- **Trigger Sensors** - select and configure the active sensor type, set the GPIO pin and hold timeout, adjust calibration (sensitivity, pulse thresholds), test wiring with live GPIO readout, invert trigger logic, and enable/disable auto-recording. See the [trigger sensors guide](./sensors) for supported sensors, wiring, and configuration details.

## How auto-recording works

When a trigger sensor is enabled:

1. The sensor fires (door opens, motion detected, vibration felt, etc.)
2. The system checks if any tracked Bluetooth or WiFi device is nearby
3. If nobody is home, recording starts automatically
4. When the sensor releases, recording continues for the configured hold timeout (default 10 seconds) to prevent gaps from brief interruptions
5. After the hold timeout, recording stops

Manual recording from the dashboard is always allowed, regardless of sensor state or presence detection.

## Storage management

The Pi's SD card will eventually fill up with recordings. The **auto-delete** feature prevents this by automatically removing the oldest recordings when disk usage exceeds a configurable threshold.

1. Go to **Settings > Storage**
2. Toggle **Auto-delete old recordings** on
3. Set the threshold (default 85%) - when disk usage exceeds this, the oldest recordings are deleted until usage drops below the limit
4. Click **Save**

The check runs before each new recording starts and every 5 minutes in the background. A disk usage bar shows your current usage relative to the threshold.

:::tip
If you have a NAS or network storage, you can use it as the recording destination. Mount the share on the Pi and select the mount point as the save location:

```bash
# Mount an NFS share
sudo mount -t nfs 192.168.1.50:/recordings /mnt/nas

# Or an SMB/CIFS share
sudo mount -t cifs //192.168.1.50/cam /mnt/nas -o guest

# Add to /etc/fstab to persist across reboots
echo '192.168.1.50:/recordings /mnt/nas nfs defaults,noatime 0 0' | sudo tee -a /etc/fstab
```

Then select `/mnt/nas` as the save location in Settings > Storage.
:::

## Troubleshooting

### Dashboard loads but no video feed

- Check MediaMTX is running: `sudo systemctl status mediamtx`
- Verify the stream directly: open `http://<pi-ip>:8889/cam` in a browser

### Cannot reach the dashboard

- Check the service is running: `sudo systemctl status security-cam`
- Make sure you're using port `3000`
- Check firewall: `sudo nft list ruleset`

### API errors in the dashboard

- Check Flask is running: `curl http://localhost:5005/system_info`

### Service fails on boot

Check the logs:

```bash
journalctl -u security-cam --no-pager -n 30
```

Common causes:
- **No internet** - `git pull` fails. The service continues with the current code, but if this is the first run and deps aren't installed, it will fail. Run `install_requirements.sh` manually once.
- **Build fails** - check Node.js is installed: `node --version`
